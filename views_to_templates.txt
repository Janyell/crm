add_edit_order {
    page_title
    order_form {
        products(вся) {
            title
            pk
            count_of_products(если нет в данном заказе, то нет и здесь)
        }
        остальное в форме есть
    }
}

order_form {
    'client.organization_or_full_name': Select(attrs={'id': "selectClient", 'required': 1}),
}


Если надо вставить какой-то элемент только на странице с название вьюхи example:
{% url 'example' as example_url %}
{% if request.path == example_url %}
    код
{% endif %}